---
interface Benefit {
  icon: string;
  title: string;
  description: string;
  features: string[];
}

const benefits: Benefit[] = [
  {
    icon: "🔒",
    title: "Seguro",
    description: "Total conformidade regulatória e transparência",
    features: [
      "Parcerias com fabricantes certificados",
      "Rastreabilidade completa",
      "Documentação oficial ANVISA",
      "Garantia de autenticidade"
    ]
  },
  {
    icon: "⚡",
    title: "Simples",
    description: "Processo descomplicado do início ao fim",
    features: [
      "Assessoria 24/7",
      "Gestão online da documentação",
      "Comunicação clara a cada etapa",
      "Suporte humanizado"
    ]
  },
  {
    icon: "🚀",
    title: "Rápido",
    description: "Agilidade sem comprometer segurança",
    features: [
      "Triagem em até 24 horas",
      "Processos otimizados",
      "Relação direta com fabricantes",
      "Acompanhamento em tempo real"
    ]
  }
];
---

<section
  id="beneficios"
  aria-labelledby="benefits-heading"
  class="relative bg-[#0a0a0a] py-24 sm:py-32 lg:py-40"
>
  <!-- Minimal background -->
  <div class="absolute inset-0 pointer-events-none">
    <div 
      class="absolute top-0 right-0 w-[600px] h-[600px] rounded-full bg-[#673de6]/5 blur-[150px]"
      aria-hidden="true"
    ></div>
  </div>

  <div class="relative z-10 max-w-6xl mx-auto px-6">
    <!-- Header - minimal -->
    <div class="text-center mb-16 sm:mb-20">
      <h2
        id="benefits-heading"
        class="text-3xl sm:text-4xl lg:text-5xl font-light text-white tracking-tight"
      >
        Por que escolher a <span class="text-[#673de6]">VitalinQ</span>?
      </h2>
      <p class="mt-4 text-white/40 max-w-xl mx-auto font-light">
        Assessoria completa na importação de medicamentos. Transformamos complexidade em simplicidade.
      </p>
    </div>

    <!-- Benefits Grid -->
    <div class="grid gap-6 md:grid-cols-3">
      {benefits.map((benefit, idx) => (
        <div 
          class="group relative p-8 rounded-2xl bg-white/5 border border-white/5 hover:border-[#673de6]/30 transition-all duration-500 hover:bg-white/[0.03]"
          style={`animation-delay: ${idx * 150}ms`}
        >
          <!-- Icon - minimal -->
          <div class="mb-6 text-2xl opacity-60 group-hover:opacity-100 transition-opacity duration-300">
            {benefit.icon}
          </div>

          <!-- Title -->
          <h3 class="text-xl font-light text-white mb-2">
            {benefit.title}
          </h3>

          <!-- Description -->
          <p class="text-white/50 text-sm mb-6 font-light">
            {benefit.description}
          </p>

          <!-- Features - minimal list -->
          <ul class="space-y-2">
            {benefit.features.map((feature) => (
              <li class="flex items-center gap-2 text-xs text-white/40">
                <span class="w-1 h-1 rounded-full bg-[#673de6]/50"></span>
                <span>{feature}</span>
              </li>
            ))}
          </ul>

          <!-- Hover line -->
          <div class="absolute bottom-0 left-8 right-8 h-px bg-gradient-to-r from-transparent via-[#673de6]/0 to-transparent group-hover:via-[#673de6]/50 transition-all duration-500"></div>
        </div>
      ))}
    </div>

    <!-- CTA - minimal -->
    <div class="mt-16 text-center">
      <a
        href="/#contato"
        class="inline-flex items-center gap-2 px-6 py-3 text-white/60 hover:text-white text-sm transition-colors group"
      >
        <span>Falar com um especialista</span>
        <span class="group-hover:translate-x-1 transition-transform duration-200">→</span>
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  section > div > div:nth-child(2) > div {
    animation: fade-in-up 0.6s ease-out forwards;
    opacity: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    section > div > div:nth-child(2) > div {
      animation: none;
      opacity: 1;
    }
  }
</style>
