<section id="passos" aria-labelledby="passos-heading" class="bg-white py-16 sm:py-20">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <h2 id="passos-heading" class="text-center text-2xl font-bold text-neutral-900 sm:text-3xl">
      [Placeholder — Como Funciona em 5 Passos]
    </h2>
    <p class="mx-auto mt-3 max-w-2xl text-center text-neutral-500">
      [Placeholder — Breve descrição de como o processo funciona, passo a passo.]
    </p>

    <ol class="mt-12 grid gap-8 sm:grid-cols-2 lg:grid-cols-5">
      {[1, 2, 3, 4, 5].map((step) => (
        <li class="flex flex-col items-center text-center">
          <span class="flex h-12 w-12 items-center justify-center rounded-full bg-primary text-lg font-bold text-white">
            {step}
          </span>
          <h3 class="mt-4 text-sm font-semibold text-neutral-800">
            [Passo {step}]
          </h3>
          <p class="mt-2 text-xs text-neutral-500">
            [Descrição do passo {step}]
          </p>
        </li>
      ))}
    </ol>
  </div>
</section>
