---
const kpis = [
  { value: "+5 anos", label: "Atuação internacional" },
  { value: "+2.000", label: "Produtos enviados" },
  { value: "7 dias", label: "Atendimento (8h-19h)" },
  { value: "100%", label: "Taxa de Sucesso" },
];

const paragraphs = [
  "Desde 2019, a VitalinQ conecta o Brasil a fabricantes globais de medicamentos, viabilizando acesso a tratamentos de alta complexidade com segurança e conformidade regulatória.",
  "Com sede nos Estados Unidos, atendemos pacientes, médicos, farmácias, hospitais e instituições.",
];
---

<section
  id="confianca"
  aria-labelledby="trust-heading"
  class="relative py-24 sm:py-32 lg:py-40 overflow-hidden"
  style="background: linear-gradient(90deg, #f8f7f5 0%, rgba(103, 61, 230, 0.02) 100%);"
>
  <!-- Decorative roxo blob -->
  <div class="absolute inset-0 pointer-events-none">
    <div
      class="absolute left-0 bottom-1/4 w-[500px] h-[500px] rounded-full bg-[#673de6]/5 blur-[150px]"
      aria-hidden="true"
    ></div>
  </div>

  <div class="relative z-10 max-w-6xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-20">
      <h2
        id="trust-heading"
        class="text-3xl sm:text-4xl lg:text-5xl font-light tracking-tight text-[#673de6]"
      >
        Confiança em cada etapa
      </h2>
      <p class="mt-4 text-gray-600 font-light max-w-xl mx-auto">
        Importação de medicamentos com segurança, documentação correta e acompanhamento claro.
      </p>
    </div>

    <!-- Two Column Layout: Info + KPIs -->
    <div class="grid gap-16 lg:grid-cols-2 items-center mb-20">
      <!-- Left: Company Info -->
      <div class="space-y-8">
        <div>
          <h3 class="text-[#673de6] text-sm font-semibold mb-3 uppercase tracking-wider">Quem somos</h3>
          <p class="text-gray-900 font-light text-lg leading-relaxed">
            {paragraphs[0]}
          </p>
        </div>
        <p class="text-gray-700 font-light leading-relaxed">
          {paragraphs[1]}
        </p>
      </div>

      <!-- Right: KPI Cards -->
      <div class="grid grid-cols-2 gap-4">
        {kpis.map((kpi) => (
          <div class="p-8 rounded-2xl bg-white border border-gray-200 hover:border-[#673de6]/40 transition-all duration-300 shadow-sm">
            <p class="text-3xl font-light text-[#673de6]">
              {kpi.value}
            </p>
            <p class="text-gray-600 text-sm mt-3 font-light">
              {kpi.label}
            </p>
          </div>
        ))}
      </div>
    </div>

    <!-- Features Grid / Value Propositions -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-20">
      <!-- Feature 1 -->
      <div class="p-8 rounded-2xl bg-white border border-gray-200 hover:border-[#673de6]/40 transition-all duration-300">
        <div class="w-12 h-12 rounded-lg bg-[#673de6]/10 flex items-center justify-center mb-4">
          <svg class="w-6 h-6 text-[#673de6]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <h3 class="text-gray-900 font-semibold text-lg mb-2">Segurança Garantida</h3>
        <p class="text-gray-600 text-sm font-light leading-relaxed">
          Conformidade total com regulamentações ANVISA e normas internacionais de segurança.
        </p>
      </div>

      <!-- Feature 2 -->
      <div class="p-8 rounded-2xl bg-white border border-gray-200 hover:border-[#673de6]/40 transition-all duration-300">
        <div class="w-12 h-12 rounded-lg bg-[#673de6]/10 flex items-center justify-center mb-4">
          <svg class="w-6 h-6 text-[#673de6]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
        </div>
        <h3 class="text-gray-900 font-semibold text-lg mb-2">Processo Rápido</h3>
        <p class="text-gray-600 text-sm font-light leading-relaxed">
          Importação ágil e eficiente, com atendimento 7 dias por semana, 8h a 19h.
        </p>
      </div>

      <!-- Feature 3 -->
      <div class="p-8 rounded-2xl bg-white border border-gray-200 hover:border-[#673de6]/40 transition-all duration-300">
        <div class="w-12 h-12 rounded-lg bg-[#673de6]/10 flex items-center justify-center mb-4">
          <svg class="w-6 h-6 text-[#673de6]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <h3 class="text-gray-900 font-semibold text-lg mb-2">Suporte Completo</h3>
        <p class="text-gray-600 text-sm font-light leading-relaxed">
          Orientação especializada em cada etapa do processo de importação do seu medicamento.
        </p>
      </div>
    </div>

    <!-- CTA Section -->
    <div class="rounded-3xl bg-gradient-to-r from-[#673de6] to-[#5a32d4] p-12 text-center">
      <h3 class="text-white text-2xl font-light mb-3">Pronto para começar?</h3>
      <p class="text-white/80 font-light mb-8">
        Entre em contato com nossos especialistas para uma avaliação gratuita da sua situação.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/#contato"
          class="inline-flex items-center justify-center gap-2 px-8 py-3 bg-white text-[#673de6] font-semibold rounded-full transition-all duration-300 hover:bg-gray-100"
        >
          Falar com especialista
        </a>
        <a
          href="/#contato"
          class="inline-flex items-center justify-center gap-2 px-8 py-3 border-2 border-white text-white font-light rounded-full transition-all duration-300 hover:bg-white/10"
        >
          Entender meu caso
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    * {
      animation: none !important;
    }
  }
</style>
