---
import logoPreta from "../../assets/images/logos/logo-preta.svg";
---

<section
  id="passos"
  aria-labelledby="steps-heading"
  class="relative py-24 sm:py-32 lg:py-40 overflow-hidden bg-[#f8f7f5]"
>
  {/* Right side triangle decoration */}
  <div
    class="absolute right-0 -bottom-1/4 pointer-events-none opacity-40"
    style={{
      width: "1500px",
      height: "1800px",
      transform: "translateY(40%)",
    }}
  >
    <svg viewBox="0 0 300 300" preserveAspectRatio="none" style={{width: "100%", height: "100%"}}>
      <polygon points="300,0 0,300 300,300" fill="#d1d5db" />
    </svg>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-16">
      <img src={logoPreta.src} alt="VitalinQ Logo" class="h-12 mx-auto mb-6" />
      <h2
        id="steps-heading"
        class="text-3xl sm:text-4xl lg:text-5xl font-light tracking-tight text-black"
      >
        5 etapas simples
      </h2>
      <p class="mt-4 text-gray-600 font-light max-w-md mx-auto">
        Você entende cada etapa. Nós cuidamos da documentação e do processo.
      </p>
    </div>

    <!-- Timeline - Progress bar style -->
    <div class="relative">
      <!-- Steps Progress Container - Desktop Horizontal -->
      <div class="mb-16 hidden lg:block">
        <!-- Progress Bar Track -->
        <div class="mb-8 flex items-center justify-between relative">
          <!-- Circle at start -->
          <div class="absolute left-0 top-1/2 transform -translate-y-1/2 z-10">
            <div class="w-3 h-3 rounded-full bg-black"></div>
          </div>

          <!-- Connecting line behind steps -->
          <div class="absolute top-1/2 left-0 right-0 h-0.5 bg-black/20 transform -translate-y-1/2" style="width: calc(100% - 20px); margin-left: 10px;"></div>

          <!-- Arrow at end -->
          <div class="absolute right-0 top-1/2 transform -translate-y-1/2 z-10">
            <svg class="w-5 h-5 text-black" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </div>

          <!-- Step indicators -->
          {[1, 2, 3, 4, 5].map((num) => (
            <div class="relative z-10 flex flex-col items-center flex-1">
              <div class="w-10 h-10 rounded-full bg-black border-4 border-[#f8f7f5] flex items-center justify-center text-white font-semibold shadow-sm">
                {num}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Mobile Vertical Timeline -->
      <div class="lg:hidden flex flex-col items-center mb-8">
        <!-- Circle at top -->
        <div class="mb-4 z-10">
          <div class="w-3 h-3 rounded-full bg-black"></div>
        </div>

        <!-- Vertical line with steps -->
        {[1, 2, 3, 4, 5].map((num, index) => (
          <>
            <div class="w-10 h-10 rounded-full bg-black border-4 border-[#f8f7f5] flex items-center justify-center text-white font-semibold shadow-sm z-10">
              {num}
            </div>
            {index < 4 && (
              <div class="w-0.5 h-12 bg-black/20"></div>
            )}
          </>
        ))}

        <!-- Arrow at bottom -->
        <div class="mt-4 z-10">
          <svg class="w-5 h-5 text-black transform rotate-90" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>

      <!-- Steps Content Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-6">
        <!-- Step 1 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left animate-fade-in-up" style="animation-delay: 0ms;">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Você envia sua receita
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Compartilhe sua prescrição médica. Nossa equipe analisa e orienta os próximos passos.
          </p>
        </div>

        <!-- Step 2 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left animate-fade-in-up" style="animation-delay: 100ms;">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Preparamos a documentação
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Cuidamos da parte regulatória conforme as exigências da ANVISA.
          </p>
        </div>

        <!-- Step 3 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left animate-fade-in-up" style="animation-delay: 200ms;">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Realizamos a importação
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Compramos diretamente de fabricantes e distribuidores internacionais homologados.
          </p>
        </div>

        <!-- Step 4 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left animate-fade-in-up" style="animation-delay: 300ms;">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Acompanhamos o processo
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Do embarque ao desembaraço aduaneiro, você recebe atualizações claras.
          </p>
        </div>

        <!-- Step 5 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left animate-fade-in-up" style="animation-delay: 400ms;">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Entrega e suporte
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Seu medicamento chega com rastreabilidade e permanecemos disponíveis para suporte.
          </p>
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="mt-16 text-center">
      <a
        href="/#contato"
        class="inline-flex items-center gap-2 px-6 py-3 bg-black text-white font-light rounded-full transition-all duration-300 hover:bg-gray-800 hover:shadow-lg"
      >
        <span>Iniciar processo</span>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.5s ease-out forwards;
    opacity: 0;
  }

  .fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
    opacity: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .fade-in-up {
      animation: none;
      opacity: 1;
    }
    .animate-fade-in-up {
      animation: none;
      opacity: 1;
    }
  }
</style>
