---
---

<section
  id="passos"
  aria-labelledby="steps-heading"
  class="relative py-24 sm:py-32 lg:py-40 overflow-hidden"
  style="background: linear-gradient(90deg, rgba(103, 61, 230, 0.03) 0%, #f8f7f5 100%);"
>
  <!-- Minimal background -->
  <div class="absolute inset-0 pointer-events-none">
    <div
      class="absolute right-0 top-1/4 w-[500px] h-[500px] rounded-full bg-[#673de6]/10 blur-[150px]"
      aria-hidden="true"
    ></div>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2
        id="steps-heading"
        class="text-3xl sm:text-4xl lg:text-5xl font-light tracking-tight text-[#673de6]"
      >
        5 etapas simples
      </h2>
      <p class="mt-4 text-gray-600 font-light max-w-md mx-auto">
        Você entende cada etapa. Nós cuidamos da documentação e do processo.
      </p>
    </div>

    <!-- Timeline - Progress bar style -->
    <div class="relative">
      <!-- Steps Progress Container -->
      <div class="mb-16">
        <!-- Progress Bar Track -->
        <div class="mb-8 flex items-center justify-between relative">
          <!-- Connecting line behind steps -->
          <div class="absolute top-1/2 left-0 right-0 h-0.5 bg-[#673de6]/20 transform -translate-y-1/2" style="width: calc(100% - 20px); margin-left: 10px;"></div>

          <!-- Step indicators -->
          {[1, 2, 3, 4, 5].map((num) => (
            <div class="relative z-10 flex flex-col items-center flex-1">
              <div class="w-10 h-10 rounded-full bg-[#673de6] border-4 border-[#f8f7f5] flex items-center justify-center text-white font-semibold shadow-sm">
                {num}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Steps Content Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-6">
        <!-- Step 1 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Você envia sua receita
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Compartilhe sua prescrição médica. Nossa equipe analisa e orienta os próximos passos.
          </p>
        </div>

        <!-- Step 2 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Preparamos a documentação
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Cuidamos da parte regulatória conforme as exigências da ANVISA.
          </p>
        </div>

        <!-- Step 3 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Realizamos a importação
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Compramos diretamente de fabricantes e distribuidores internacionais homologados.
          </p>
        </div>

        <!-- Step 4 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Acompanhamos o processo
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Do embarque ao desembaraço aduaneiro, você recebe atualizações claras.
          </p>
        </div>

        <!-- Step 5 -->
        <div class="flex flex-col items-center sm:items-start text-center sm:text-left">
          <h3 class="text-gray-900 font-semibold text-base mb-2">
            Entrega e suporte
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Seu medicamento chega com rastreabilidade e permanecemos disponíveis para suporte.
          </p>
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="mt-16 text-center">
      <a
        href="/#contato"
        class="inline-flex items-center gap-2 px-6 py-3 bg-[#673de6] text-white font-light rounded-full transition-all duration-300 hover:bg-[#5a32d4] hover:shadow-[0_0_30px_rgba(103,61,230,0.3)]"
      >
        <span>Iniciar processo</span>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>
  </div>

  <!-- Horizontal fade effect from roxo to off-white -->
  <div class="absolute right-0 top-0 bottom-0 w-40 pointer-events-none" style="background: linear-gradient(90deg, transparent, #f8f7f5);"></div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
    opacity: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .fade-in-up {
      animation: none;
      opacity: 1;
    }
  }
</style>
