---
const testimonials = [
  {
    id: 1,
    name: "Dr. Ricardo Mendes",
    role: "Médico Oncologista",
    company: "Hospital Sírio-Libanês",
    content: "A VitalinQ foi essencial para conseguir medicamentos oncológicos importados para meus pacientes. O processo foi rápido, seguro e totalmente transparente.",
  },
  {
    id: 2,
    name: "Ana Paula Silva",
    role: "Paciente",
    company: "Tratamento de Esclerose Múltipla",
    content: "Depois de meses procurando um medicamento específico, a VitalinQ resolveu tudo em menos de 2 semanas. Profissionalismo e empatia do início ao fim.",
  },
  {
    id: 3,
    name: "Farmácia Saúde Total",
    role: "Responsável Técnico",
    company: "São Paulo - SP",
    content: "Parceria confiável para importação de medicamentos especiais. Documentação impecável e suporte técnico excepcional.",
  },
];
---

<section
  id="depoimentos"
  class="relative bg-[#0a0a0a] py-24 sm:py-32 lg:py-40 text-white overflow-hidden"
>
  <!-- Minimal background -->
  <div class="absolute inset-0 pointer-events-none">
    <div 
      class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full bg-[#673de6]/5 blur-[200px]"
      aria-hidden="true"
    ></div>
  </div>

  <div class="relative z-10 max-w-6xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-light tracking-tight">
        Confiança de quem já importou
      </h2>
      <p class="mt-4 text-white/40 font-light max-w-xl mx-auto">
        Médicos, pacientes e farmácias confiam na VitalinQ
      </p>
    </div>

    <!-- Testimonials Grid -->
    <div class="grid gap-6 md:grid-cols-3">
      {testimonials.map((testimonial) => (
        <div class="p-8 rounded-2xl bg-white/5 border border-white/5 hover:border-[#673de6]/20 transition-all duration-500">
          <!-- Stars -->
          <div class="flex gap-1 mb-6">
            {[...Array(5)].map((_, i) => (
              <svg class="w-4 h-4 text-[#673de6]" fill="currentColor" viewBox="0 0 20 20">
                <path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z" />
              </svg>
            ))}
          </div>

          <!-- Content -->
          <p class="text-white/70 font-light leading-relaxed mb-6">
            "{testimonial.content}"
          </p>

          <!-- Author -->
          <div class="border-t border-white/10 pt-4">
            <p class="text-white font-light">{testimonial.name}</p>
            <p class="text-white/40 text-sm mt-1">{testimonial.role}</p>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <div class="mt-16 text-center">
      <a
        href="/#contato"
        class="inline-flex items-center gap-2 px-6 py-3 text-white/60 hover:text-white text-sm transition-colors group"
      >
        <span>Iniciar minha importação</span>
        <span class="group-hover:translate-x-1 transition-transform duration-200">→</span>
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  section > div > div:nth-child(3) > div {
    animation: fade-in-up 0.6s ease-out forwards;
    opacity: 0;
  }

  section > div > div:nth-child(3) > div:nth-child(1) { animation-delay: 0ms; }
  section > div > div:nth-child(3) > div:nth-child(2) { animation-delay: 100ms; }
  section > div > div:nth-child(3) > div:nth-child(3) { animation-delay: 200ms; }

  @media (prefers-reduced-motion: reduce) {
    section > div > div:nth-child(3) > div {
      animation: none;
      opacity: 1;
    }
  }
</style>
